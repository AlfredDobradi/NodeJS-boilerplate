{
	"name": "",
	"version": "0.0.1",
	"dependencies": {
		"mongoose": "latest",
		"express": "latest",
		"jade": "latest"
	},
	"devDependencies": {
		"grunt": "latest",
		"grunt-cli": "latest",
		"grunt-contrib-jshint": "latest",
		"bower": "latest"
	},
	"scripts": {
		"postinstall": "if [ -f bower.json ]; then echo bower.json found, running bower install; bower install; else echo bower.json not found; fi;"
	}
}